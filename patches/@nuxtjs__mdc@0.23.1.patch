diff --git a/dist/runtime/components/MDCRenderer.vue b/dist/runtime/components/MDCRenderer.vue
index ac1f41315e7a97eee78a2665b9ebd844fe14989e..4b1857d29a27ecede8e70cfa25f44da0b4193c26 100644
--- a/dist/runtime/components/MDCRenderer.vue
+++ b/dist/runtime/components/MDCRenderer.vue
@@ -306,8 +306,10 @@ const resolveComponentInstance = (component) => {
     if (typeof _component === "string") {
       return _component;
     }
+    // Sync setup() components must hydrate immediately. An async wrapper
+    // replaces SSR <h1>/<p> with unknown <proseh1>/<prosep> tags.
     if ("setup" in _component) {
-      return defineAsyncComponent(() => new Promise((resolve) => resolve(_component)));
+      return _component;
     }
     return _component;
   }
